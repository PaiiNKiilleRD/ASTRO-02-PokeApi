---
interface Props {
  name: string;
  url: string;
  isBig?: boolean;
}

const { name, url, isBig = false } = Astro.props;
const id = url.split("/").at(-2);
const imageSrc = `https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/other/official-artwork/${id}.png`;
---

<a
  href={`/pokemon/${name}`}
  class:list={[
    `rounded flex flex-col justify-center items-center p-2 
    [&:hover>img]:scale-110 hover:border-sky-500 transition-all duration-300 ease-in-out`,
    { border: !isBig },
  ]}
>
  <img
    transition:name={`${name}-image`}
    src={imageSrc}
    alt={name}
    class={`${isBig ? "w-26 h-26" : "w-22 h-22"} transition-all duration-300 ease-in-out`}
  />
  <span class={`capitalize ${isBig ? " text-3xl" : ""}`}>#{id} {name}</span>
</a>
